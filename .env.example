# Copy to `.env` and fill in real values. `.env` is git-ignored.

# --- PostgreSQL (used by both the app and the integration tests) ---
POSTGRES_USER=midia
POSTGRES_PASSWORD=12345@Ab
POSTGRES_DB=MidIADataBase

# --- App ---
ASPNETCORE_ENVIRONMENT=Production
APP_PORT=8080

# --- Groq ---
# Required at runtime. Get one at https://console.groq.com/keys
GROQ_API_KEY=placeholder
